user:
  name: ishikki
  github: ishikki-akabane

python_version: "3.10"

commands:
  build:
    - "pip3 install -r requirements.txt"
  package:
    - "nano inxi"
  start:
    - "python3 bot.py"

env:
  - "ENVIRONMENT=production"
